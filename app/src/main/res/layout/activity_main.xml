<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/text_activity_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/activity_type" />

    <Spinner
        android:id="@+id/spinner_activity"
        android:layout_width="130dp"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/text_activity_type"
        android:prompt="@string/hint_select_activity"
        android:spinnerMode="dialog" />

    <Button
        android:id="@+id/button_activity_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/spinner_activity"
        android:text="@string/activity_add" />

    <Button
        android:id="@+id/button_start"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_activity_type"
        android:layout_marginTop="50dp"
        android:text="@string/start" />

    <TextView
        android:id="@+id/text_status"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/button_start"
        android:layout_marginTop="20dp"
        android:width="300dp"
        android:textColor="#FF0000"
        android:textSize="20sp" />

</RelativeLayout>
